# Tokenization 

- strtok() returns each (HEAD,WEIGHT) edge 

- graph[graphIndex].edge is set to NULL which would result in First found edge's next ptr to point to NULL 

- Each new edge token is appended to beginning of list (last found edge = first edge in edge list of a node) 
  